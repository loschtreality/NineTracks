import React, { PropTypes } from 'react'

class PlayBarButton extends React.Component {
  render () {
    return (
      <span className="min_button_description">Play Playlist</span>
    );
  }
}

export default PlayBarButton;
